<% @comments.map do |comment, nested_comments| %>
  $('#images-comments-<%= @image.id %>')
    .append('<%= j render "comment", comment: comment %>');
  $('#images-comments-<%= @image.id %>')
    .append('<%= j render "edit_form", comment: comment %>');
<% end %>
$('#load-more-image-comments-<%= @image.id %>').hide();
