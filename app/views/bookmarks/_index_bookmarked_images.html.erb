<div class="news-feed-middle">
  <div class="news-feed-middle-content with-padding">
    <% if images.any? %>
      <div id="news-feed-images">
        <% images.each do |image| %>
          <%= render "images/image", image: image %>
        <% end %>
      </div>
      <div id="load-more-news-feed" class="load-more-news-feed">
        <% if Image.bookmarked_images(user).size > images.size %>
          <%= link_to t("view-more"), user_bookmarks_path(user,
            image_offset: images.last.id), remote: true %>
        <% end %>
      </div>
    <% else %>
      <h3 class="no-content"><%= t :no_content %></h3>
    <% end %>
  </div>
</div>
